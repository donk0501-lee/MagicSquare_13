# Refactor Commit #1 Report (R-L4)

본 문서는 **Dual-Track(REFACTOR) 1커밋 단위**로 수행한 리팩토링 내역을 기록합니다.  
규칙: **기능 변경 금지 / 계약 변경 금지 / 테스트 약화 금지 / 항상 GREEN 유지**.

## 1) 이번 커밋 리팩토링 목표 (선택한 항목 ID)

- **R-L4**: `solution()`에서 “두 조합 시도(try1/try2)” 중복을 `try_placement(order)` 형태로 분리

## 2) 변경 범위 요약 (UI Track / Logic Track)

- **UI Track**: 변경 없음
- **Logic Track**: `src/magic_square/logic.py`의 `solution()` 내부 중복 제거(시도 로직 helper 추출)

## 3) 변경 전 문제점 → 변경 후 개선점

### 변경 전 문제점

- `solution()`에서 동일한 패턴(격자 채우기 → 마방진 판정 → 성공 시 반환)이 2회 반복되어 가독성이 떨어짐
- 정책/반환 스키마를 유지하면서 내부 구현만 바꾸더라도, 중복 코드로 인해 수정 누락 위험이 존재

### 변경 후 개선점

- `try_placement(n1, n2)`로 “시도”를 단일 단위로 만들어 중복 제거
- **시도 순서**, **반환 포맷**(`int[6]`, 1-index), **예외 타입/메시지**는 그대로 유지

## 4) 수정된 파일 목록

- **수정**
  - `src/magic_square/logic.py`
- **추가/이동**: 없음

## 5) 테스트 실행 결과 요약

- **Step 0 (기준선 확인)**  
  - 실행: `python -m pytest`  
  - 결과: **PASS** (`43 passed`)

- **Step 4 (리팩토링 후 회귀 확인)**  
  - 실행: `python -m pytest`  
  - 결과: **PASS** (`43 passed`)

## 6) 위험 요소 및 롤백 포인트

- **위험 요소**
  - `try_placement`가 `solution()` 내부에서 클로저로 `matrix/i1/j1/...` 값을 캡처하므로, 스코프/변수명 변경 시 실수 가능

- **롤백 포인트**
  - `src/magic_square/logic.py`의 `solution()` 내부 helper 도입 부분만 제거하고, 기존의 `grid_try1/grid_try2` 직접 코드로 복원하면 됨(파일 1곳/함수 1곳)

## 7) 커밋 메시지 제안 (Conventional Commit)

- `refactor(domain): extract placement attempt helper in solution`

